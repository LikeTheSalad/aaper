import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal()
        mavenCentral()
        google()
    }
}
buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.likethesalad.tools:artifact-publisher:1.2.2'
        classpath 'com.likethesalad.tools.testing:android-compilation-testing-plugin:2.0.0.1'
    }
}
rootProject.name = 'Annotated permission'
include ':app'
include ':aaper-api'
include ':aaper'
include ':aaper-plugin'